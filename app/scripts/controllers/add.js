// Generated by CoffeeScript 1.7.1
(function() {
  'use strict';
  angular.module('bracketsApp').controller('AddCtrl', function($scope, $http, $location) {
    $scope.submit = function(form) {
      $scope.submitted = true;
      if (form.$invalid) {
        toastr.error('Errors', 'Failed');
        return;
      }
      toastr.info('Processing request...', 'Working...');
      $http.post('/api/addBracket', JSON.stringify({
        name: $scope.name,
        user: $scope.currentUser
      })).success(function(result) {
        toastr.success('Successfully Added!', 'Success');
        $location.path('/view/' + result._id);
      });
    };
  });

}).call(this);

//# sourceMappingURL=add.map
